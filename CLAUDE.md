# CLAUDE.md

This file is a legacy compatibility entry for tools that still discover `CLAUDE.md`.

The canonical repository instructions are in [`AGENTS.md`](./AGENTS.md). Follow that file for the current Domi architecture, commands, code style, testing, packaging and Agent Runtime constraints. Do not maintain a second copy of those rules here.

Domi itself uses `AGENTS.md` as the writable project-instruction format. A legacy `CLAUDE.md` in a user workspace is migration input only; it is not evidence that Claude Agent SDK remains an execution runtime.
