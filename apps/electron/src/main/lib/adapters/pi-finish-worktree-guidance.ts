export const PI_FINISH_WORKTREE_GUIDANCE = {
  description: 'Create one isolated task commit in Local and by default clean the current managed Worktree; the interactive confirmation lets the user optionally retain the frozen runtime environment for 24 hours, 3 days, or manual cleanup. Use only when the user explicitly says to skip Local review and submit directly. Before calling it, write the full change summary and validation results into your reply text; the confirmation shows only one final cumulative commit message. Derive that editable commit message from the net diff between the current effective review baseline and the final Worktree snapshot, including all unpublished checkpoints and committed, staged, unstaged, and untracked changes. Use the original delivery baseline unless the host exposes an integrated Local review base; never include changes already present in that Local base. The main feature must lead the subject and primary bullets; the latest wording/order/style micro-adjustment is secondary. Never concatenate historical or checkpoint commit messages, never summarize only the latest user message, and do not emit duplicate bullets. Generate the message in the user conversation’s primary language as a concise subject, a blank line, and 2–5 concrete hyphen bullet points; technical identifiers and Conventional Commit type/scope may stay unchanged. This requires one host confirmation and must never be used unattended.',
  promptSnippet: 'FinishWorktree: only after explicit user intent to skip review and submit directly, create one task commit; default to retention=cleanup and let the user change runtime retention in the confirmation. Before the call, inspect the changed files/diff from the effective review baseline to the final Worktree snapshot, covering unpublished checkpoints plus committed, staged, unstaged, and untracked changes, then write the full delivery change summary and verification results in your reply text. When the host exposes an integrated Local review base, exclude everything already present in that base. Pass one final cumulative commit message with the main feature in the subject and primary bullets; the latest micro-adjustment is secondary. Never concatenate historical or checkpoint commit messages, rely only on the latest user message, or duplicate bullets. The message must follow the user’s primary language and use a subject + blank line + 2–5 detailed hyphen bullets. Never infer permission merely because implementation is complete.',
} as const
