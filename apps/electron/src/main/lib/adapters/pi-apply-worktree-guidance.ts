export const PI_APPLY_WORKTREE_CONFLICT_GUIDANCE = {
  description: 'Project the current owner managed Worktree into Local as one durable, reversible Preview. Use only when the user explicitly asks to apply, write back, or deliver. A successful call enters Local review and must be finalized or withdrawn before further Worktree edits, preventing untracked Apply layers. Never edit Local directly. If this returns a conflict, integrate the returned localHeadOid into the current Worktree, resolve and verify there, then treat that Local HEAD as the effective review baseline and call ReadyForReview to generate a fresh snapshot-bound review card containing only the remaining delivery delta. Do not call ApplyWorktree again or call FinishWorktree unless the user later explicitly asks to skip review and submit directly.',
  promptSnippet: 'ApplyWorktree: after explicit user intent, project the whole task into Local as a reversible Preview. Success enters Local review; do not continue editing until the Preview is finalized or withdrawn. On conflict, resolve inside the current Worktree against localHeadOid, use that integrated Local HEAD as the effective review baseline, verify, then end with ReadyForReview so the user receives a fresh review card containing only the net delivery delta; do not retry ApplyWorktree or use FinishWorktree.',
  resultMessage: 'Apply 检测到真实冲突。请在当前 managed Worktree 内同步 localHeadOid、解决冲突并验证，把该 Local HEAD 作为有效验收基线，仅总结其后尚未进入 Local 的净增量，然后调用 ReadyForReview 生成新的快照绑定验收卡；不要再次调用 ApplyWorktree，不要调用 FinishWorktree，也不要直接修改 Local checkout。',
} as const
